spring:
  application:
    name: spring-cloud-gateway
  profiles:
    active: dev
  cloud:
    config:
      fail-fast: true
      uri: http://config:9201
      profile: ${spring.profiles.active}
        #discovery:
      # enabled: true
      #service-id: config
      label: ${spring.profiles.active}
      enabled: true

